import 'package:flutter/material.dart';

class SchemesScreen extends StatelessWidget {
  const SchemesScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Government Schemes')), body: const Center(child: Text('PM-KISAN, KCC, subsidies with eligibility checks.')));
}
