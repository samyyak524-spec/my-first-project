import 'package:flutter/material.dart';

class TractorScreen extends StatelessWidget {
  const TractorScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Tractor Advice')), body: const Center(child: Text('Implements and field prep strategy assistant.')));
}
